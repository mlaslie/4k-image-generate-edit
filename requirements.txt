# Runtime dependencies are defined once, in the deployed agent package.
-r image_agent/requirements.txt

# Local development / test only (not deployed).
pytest>=8.0.0
pytest-asyncio>=0.24.0
anyio>=4.0.0
